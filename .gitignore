node_modules
dist
.DS_Store
*.local
.env
.env.*
!.env.example

# CodeGraph index (local, do not commit)
.codegraph/

# Local chain config (points at a private cluster; each dev supplies their own)
src/chain/config.json
